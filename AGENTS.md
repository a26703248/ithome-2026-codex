# AGENTS.md — 專案撰寫與實驗規範

本檔是本專案（iThome 鐵人賽系列《Java 工程師的 AI 協作實驗室：30 天打造可驗證的 ChatGPT 與 Codex 開發工作流》）的共同作業規範。

撰寫文章、設計實驗、修改程式、產生圖片或提交版本前，應先閱讀本檔並逐條遵守。系列不以證明 AI 萬能為目標，而是透過可重現實驗，釐清 ChatGPT、OpenAI API、Codex 與人工審查在 Java 開發流程中的合理責任邊界。

---

## 專案概要

- **競賽主題**：ChatGPT & Codex。
- **系列定位**：Java 工程師以同一個 Spring Boot 專案，逐步驗證 AI 在需求整理、結構化輸出、外部工具、程式修改、測試、Code Review、資安、法規與人工核准中的可用範圍。
- **系列名稱**：**Java 工程師的 AI 協作實驗室：30 天打造可驗證的 ChatGPT 與 Codex 開發工作流**。
- **一句話主張**：不只問 AI 能不能寫程式，而是使用固定案例、測試、Git diff、人工評分與稽核紀錄，驗證 AI 在真實 Java 工作流中能負責什麼。
- **核心專案**：**AI 輔助問題追蹤與開發助手（AI Dev Workflow Assistant）**。
- **主要讀者**：具備一般軟體開發概念的 Java 工程師、技術主管、專案經理，以及想理解企業 AI 工作流的讀者。
- **文章語言**：繁體中文。
- **文章風格**：務實、專業、符合著作權、可驗證、可重現，適合工程師閱讀。
- **核心原則**：不假裝已經是專家；完整記錄問題、資料查證、實驗、失敗、修正、人工判斷與有限結論。
- **最終產出**：
  - 一個可執行的 Spring Boot 專案。
  - Issue 整理與 Stack Trace 分析 REST API。
  - 結構化輸出 Schema、Prompt 版本與固定測試資料集。
  - 白名單 Tool Calling 與人工核准流程。
  - Codex 任務紀錄、Git diff、JUnit 驗證與修改驗收清單。
  - 一份包含成功、失敗、風險、法規查證與企業導入邊界的總結報告。

## 系列核心問題

整個系列要持續回答：

> ChatGPT、OpenAI API 與 Codex 在 Java 開發流程中，哪些工作可以委派、哪些只能提供建議、哪些必須由人決定？

所有文章應能回到以下主線：

```text
模糊問題輸入
→ ChatGPT／Prompt 實驗
→ 結構化 Issue JSON
→ Spring Boot 呼叫 OpenAI API
→ 內部資料檢索與有限工具呼叫
→ Codex 閱讀、修改與測試專案
→ JUnit、Git diff 與人工 Review
→ 人工核准後才允許執行
→ 稽核、評估、法規與限制
```

## 30 天階段規劃

完整文章表格放在 [README.md](README.md)。各階段的學習與實作邊界如下：

1. **Day 1–5：問題定義與基礎能力**
  - 建立固定 Issue 測試樣本。
  - 區分 AI 建議與人工責任。
  - 測試輸出變異。
  - 建立品質評分標準與實驗紀錄格式。

2. **Day 6–10：Prompt 與結構化輸出**
  - 比較不同 Prompt 設計。
  - 處理資料不足與未知欄位。
  - 建立 JSON Schema。
  - 驗證格式錯誤、業務錯誤與重試策略。

3. **Day 11–15：API、RAG、工具呼叫與外部系統**
  - 使用 Spring Boot 呼叫模型 API。
  - 管理金鑰、Prompt 與模型設定。
  - 將回應安全轉換為 Java DTO。
  - 建立最小文件檢索。
  - 實作白名單工具與 Tool Calling 提案流程。

4. **Day 16–20：Codex 與程式碼任務**
  - 建立 Codex 任務契約。
  - 分析與修正刻意植入的 Bug。
  - 比較「只提建議」與「直接修改」。
  - 限制可修改模組與檔案。
  - 建立 AI 程式碼修改驗收清單。

5. **Day 21–25：測試、除錯、Code Review、安全與法規**
  - 驗證 AI 產生的 JUnit 是否真的測到需求。
  - 使用錯誤訊息與 Stack Trace 建立可驗證假設。
  - 比較 AI Review 與人工 Review。
  - 實作敏感資訊遮罩。
  - 查證個資、著作權、服務條款與企業政策。

6. **Day 26–29：整合專案**
  - 整合 Issue 分析 API。
  - 整合 Stack Trace 與內部規範檢索。
  - 實作人工核准狀態流程。
  - 使用固定資料集執行回歸評估。

7. **Day 30：成果、限制、反思與下一步**
  - 彙整量化指標。
  - 公開失敗案例與限制。
  - 說明哪些流程可輔助、哪些不可自動化。
  - 提出企業內部試辦的下一階段路線。

---

## 產品與技術邊界

文章必須清楚區分以下概念，不得混用：

| 名稱 | 本系列中的定義 | 不可誤寫成 |
|---|---|---|
| ChatGPT | 使用者操作的 AI 產品與多模態對話介面 | OpenAI API 的同義詞 |
| OpenAI API | 應用程式透過 HTTP 或 SDK 呼叫模型與工具能力的開發介面 | ChatGPT 網頁功能 |
| Codex | 用於閱讀、修改、執行與驗證軟體專案任務的 coding agent／產品能力 | 單純的程式碼生成模型 |
| 模型 | 實際進行文字生成、推理或工具選擇的底層模型 | 完整產品、IDE 或 Agent |
| IDE／CLI | 使用 Codex 或其他工具的操作介面 | 模型本身能力 |
| 第三方框架 | Spring AI、LangChain4j 等非 OpenAI 官方套件 | OpenAI 官方 API 規格 |
| RAG | 應用程式檢索資料並將內容提供給模型的設計方法 | 模型已自動知道公司資料 |
| Tool Calling | 模型提出結構化工具呼叫要求的機制 | 工具已執行或已取得權限 |
| Agent | 能依目標規劃並使用工具完成多步任務的系統 | 無限制、無監督的自動化 |

每篇文章首次提及產品或能力時，應說明本次實驗實際使用的是哪一個產品、介面、模型、SDK 或第三方框架。

---

## 核心專案設計與功能邊界

### 專案名稱

- 中文：**AI 輔助問題追蹤與開發助手**
- 英文：**AI Dev Workflow Assistant**
- 建議 Repository：`ai-dev-workflow-assistant`

### 專案目標

建立一個可示範下列流程的 Spring Boot 應用程式：

1. 接收模糊的使用者問題。
2. 產生結構化 Issue 草稿。
3. 標記已知事實、推測、未知資訊與待追問項目。
4. 分析 Java Stack Trace 並提出可驗證假設。
5. 查詢有限範圍的內部規範。
6. 產生白名單工具的呼叫提案。
7. 記錄 Prompt、回應、驗證與人工決策。
8. 未經人工核准，不執行具有副作用的操作。

核心流程：

> AI 建議 → 程式驗證 → 人工核准 → 有限執行 → 完整稽核

### 建議模組

```text
ai-dev-workflow-assistant
├── app-api
│   ├── issue
│   ├── diagnostic
│   └── approval
├── app-domain
│   ├── IssueDraft
│   ├── DiagnosticReport
│   ├── ToolProposal
│   └── ApprovalDecision
├── app-ai
│   ├── AiGateway
│   ├── PromptTemplate
│   ├── StructuredOutputParser
│   └── ModelResponseValidator
├── app-retrieval
│   ├── DocumentLoader
│   ├── Retriever
│   └── CitationBuilder
├── app-tools
│   ├── ToolRegistry
│   ├── SafeTool
│   └── RestrictedExecutor
├── app-policy
│   ├── SensitiveDataDetector
│   ├── InputSanitizer
│   └── ExecutionPolicy
├── app-audit
│   ├── ExperimentRecord
│   ├── AiInteractionLog
│   └── EvaluationResult
└── app-test-support
    ├── Fixtures
    ├── FakeAiGateway
    └── EvaluationDataset
```

優先使用 Modular Monolith，不因展示 AI 而過早拆成微服務。

### 最小可行版本

第一階段只實作：

- `POST /api/issues/analyze`
- Issue Structured Output 驗證
- Prompt、回應與人工評分紀錄
- 固定測試資料集與 JUnit 測試

### 30 天內合理功能

- Issue 分析 API。
- Stack Trace 診斷 API。
- Java DTO、JSON Schema 與業務驗證。
- 最小文件檢索。
- 二至三個唯讀或模擬工具。
- 人工核准狀態流程。
- Codex 任務與修改紀錄。
- 固定評估資料集與回歸報告。
- 敏感資料遮罩與輸入檢查。

### 不實作或禁止實作

- 自動合併 Pull Request。
- 自動部署正式環境。
- 無限制 Shell 或網路存取。
- AI 自行取得、提升或擴張權限。
- AI 單方面決定正式 Issue 優先級。
- 使用真實客戶個資或公司機密程式碼做公開 Demo。
- 自動給出法律合規結論。
- 複雜多 Agent 平台。
- 模型訓練或 Fine-tuning。
- 為展示而建立不必要的微服務。
- 宣稱「完全自動化」但實際仍需要大量人工操作。

---

## 檔案與目錄結構

- **`DayNN.md`（根目錄）**：30 篇文章（`Day01.md`–`Day30.md`）。檔案直接從 `# Day NN：標題` 開始，不要放 YAML frontmatter（`--- ... ---`）——那會被 GitHub／ITHome 渲染成表格，正式文章不需要。成品要能整份直接貼上 ITHome 發布。撰稿時直接編輯對應檔案。
- **`程式碼/DayNN/`**：該天實作文章的可執行程式碼（見「實作 demo」慣例）。
- **`實驗/DayNN/`**：該天的輸入、輸出、評分、測試結果與失敗紀錄。
- **`圖檔/DayNN/`**：每天文章的圖片目錄，當天所有圖片放進對應資料夾（見下方「文章配圖」的命名規則）。
- **`資料集/`**：固定且去識別化的 Issue、Stack Trace、Prompt Injection 與工具選擇案例。
- **`提示詞/`**：版本化 Prompt、System Instruction、Tool Description 與輸出 Schema。
- **`規範/`**：自製的內部政策、開發規範與 RAG 測試文件；不得放真實公司機密。
- **`參考/`**：陸續蒐集的條文與素材（如《人工智慧基本法》、OWASP 等 PDF），僅供本機查閱與寫作對照。屬單人參考用途；受著作權的原文不得轉貼進文章，也不應隨公開版本（如 GitHub）散布（見著作權紅線）。
- **`審查/DayNN/`**：每篇文章的三角色審查報告與修正紀錄。
- **`README.md`**：30 天大綱、進度、執行方式與成果索引。
- **`AGENTS.md`**：本規範。

建議的單日實驗目錄：

```text
實驗/DayNN/
├── README.md
├── input/
├── output/
├── evidence/
├── result.csv
└── notes.md
```

---

## 著作權紅線：公開發表，從嚴處理

**本系列文章會公開發表在 ITHome（公開網頁），所以一律採最嚴格標準。** 核心判準不變：**著作權保護「表達（文字）」，不保護「概念、事實、方法、系統」。** 用自己的話講它的「意思」＝安全；搬它的「字」＝危險。

依來源把「可不可以引用原文」分成三層，落筆前先判斷手上的素材屬於哪一層：

### 第一層：ISO / CNS 標準 → 綠燈 only，不逐字引用

ISO 與 CNS 是**受著作權的私權文件**（購買僅取得「閱讀與實施」授權，不含重製散布）：

- ISO 官方版權政策明訂：未經書面許可，禁止以任何形式（**含網路公開**）複製、儲存或傳輸標準的任何部分（[ISO Copyright](https://www.iso.org/privacy-and-copyright.html)）。
- CNS 著作權屬經濟部標準檢驗局（[BSMI](https://www.bsmi.gov.tw/)），僅供內部參考／教學對照；**在公開網頁直接貼出條文段落，即使短句，仍有極高侵權風險。**

因此對 ISO/CNS，本系列**只走綠燈**——**逐字引用（含短句、含自行翻譯）一律視同紅燈**：

- ✅ 用**自己的話**解釋條款要求什麼、為什麼重要。
- ✅ 討論**架構**：章節編號與標題屬事實（如「主體條款 4–10」「附錄 A 有哪幾類控制」），可寫。
- ✅ 加上**原創詮釋、範例、對映表**（把某條控制對映到自建 RAG 做法）——最有價值也最安全。
- ✅ 使用官方繁中**術語／名詞**（單一詞彙非受保護的表達），確保用語一致。
- ✅ 一律**註明出處與版本**（如「依 ISO/IEC 42001:2023 第 6 章」）。引用出處本身不侵權。

### 第二層：法律條文 → 可自由引用（如《人工智慧基本法》）

台灣《著作權法》§9 明定憲法、法律、命令、公文**不得為著作權標的**。所以**法律條文（如《人工智慧基本法》全文、施行細則、各部會作用法）可直接引用、全文轉錄**，仍建議註明條號與出處。Day 6–8 基本法章節可放心引用條文原文。

### 第三層：開放／公開授權來源 → 依授權引用、改作

要「引用原文」時，用這些，不要用 ISO/CNS：

- **OWASP Top 10 for LLM Applications 2025**：文件明訂採 **創用 CC 姓名標示-相同方式分享 4.0（CC BY-SA 4.0）**，可自由分享與改作（含商業用途）。兩個條件：**姓名標示**（標註 OWASP、附授權連結、若有修改要註明）＋**相同方式分享（ShareAlike）**（若「改作」其內容，該衍生部分須以同一 CC BY-SA 4.0 授權釋出）。實務判斷：用**自己的話**寫十大風險＝不觸發 ShareAlike（概念／事實不受授權拘束）；**直接引用或翻譯**其原文段落＝屬改作，需標註授權並注意 ShareAlike。建議標註：「改寫自 OWASP Top 10 for LLM Applications 2025，CC BY-SA 4.0」＋連結。
- **政府公開資料**：國科會／數位部官方導引與摘要、AIEC 公開評測項目說明、政府新聞稿與公開簡報——引用並註明出處。

### 🔴 一律禁止

- 逐字照抄或整段翻譯 **ISO/CNS** 條文（尤其附錄 A 控制清單原文，最受保護）。
- 貼出／轉發購買的 **PDF、標準內表格與圖**，或把 `參考/` 內受版權檔案提交進公開版控。
- 分享標準檔案（單人使用授權）。

### ✅ 落筆前自我檢查

> 這段素材是 ISO/CNS 嗎？是 → 只能用自己的話，不逐字引。
> 若有人光看文章就不必買標準 → 抄太多了，退回重寫。
> 若看完反而更想買正本、只是更懂怎麼用 → 過關。

### OpenAI、Java 與第三方技術文件的補充規則

- OpenAI、Oracle、Spring、JUnit、Git、JSON Schema 與第三方框架文件仍受著作權保護；原則上只引用必要短句，其餘均以自己的話解釋。
- API Request／Response 範例應優先自行撰寫，不整段複製官方教學。
- 官方程式碼範例如需改作，必須確認其授權、保留必要標示，並清楚說明修改內容。
- 不將 AI 產生的文章或程式碼直接視為「無著作權風險」；發布前仍需比對來源、檢查相似性與授權。
- 文章中的 Prompt、資料集、架構圖與測試案例應盡量自行設計，建立系列的原創價值。
- 法律適用與合理使用判斷具有情境性；無法確認時標記「待法律專業人士確認」，不得自行補完。

---

## 法規、條款與資料政策查證

本系列的法規與產品政策不是裝飾性章節，而是企業 AI 工作流的限制條件。

涉及下列資訊時，發布前必須重新查閱最新官方文件並標示查閱日期：

- ChatGPT 產品功能與方案差異。
- OpenAI API 端點、SDK、模型名稱、參數與限制。
- Structured Outputs、Function Calling、Tool Calling 的正式行為。
- Codex 的操作介面、權限、sandbox、approval 與 network access。
- API 價格、速率限制、資料保留與訓練政策。
- OpenAI 使用條款、服務條款與使用政策。
- Java、Spring Boot、JUnit、Git 與第三方框架版本。
- iThome 鐵人賽當年度投稿規則。
- 臺灣個人資料保護法、著作權法及主管機關發布的解釋或指引。
- 企業內部資訊安全、資料分類、委外與開源軟體政策。

資料來源優先順序：

1. OpenAI 官方文件、Help Center、產品公告、Changelog、條款與政策。
2. Java、Spring、JUnit、Git、JSON Schema 等官方文件或原始規格。
3. 法務部全國法規資料庫與政府機關正式資料。
4. 原始研究論文、RFC、標準或技術規格。
5. 可信任技術文章。
6. 社群討論，只能作為經驗線索，不得單獨作為事實依據。

每項動態資訊需記錄：

```text
資料名稱：
發布單位：
文件類型：
查閱日期：
文件版本或更新日期：
本文使用範圍：
是否仍有待查證事項：
```

文章中不得把「模型回答」當成官方文件，也不得因 AI 回答附帶網址就跳過人工開啟與核對。

---

## 每篇開頭的定位聲明（固定小字）

每篇文章在 `# Day NN：標題` 正下方、階段引言之上，固定插入：

```markdown
> 📝 *本系列為 iThome 鐵人賽的工程實驗與學習紀錄；文中 AI 輸出均須經程式驗證、人工審查或官方資料查證，不將模型回答直接視為事實。涉及法規、標準與第三方文件時，以自己的話轉述並註明出處。*
```

此聲明不是免責條款，也不會取代著作權、資料保護、事實查核與人工核准。

---

## 每篇文章的核心研究流程

每篇文章都應依序完成下列十項工作：

1. **今天要回答的核心問題**
  - 使用可被實驗驗證的問句。
  - 避免只用「介紹某名詞」作為題目。

2. **需要先理解的概念**
  - 列出今日必須理解、可延後與本篇不處理的概念。

3. **建議搜尋的中英文關鍵字**
  - 中英文都要準備，避免只依賴二手中文內容。

4. **優先閱讀的官方資料**
  - 明確列出文件類型、版本與查閱日期。

5. **可以動手驗證的小實驗**
  - 每篇至少有一個可重現操作。

6. **判斷結果正確與否的方法**
  - 使用 Schema、JUnit、Git diff、固定 Rubric、錯誤碼或人工核准紀錄。

7. **常見誤解**
  - 每篇至少說明一個容易誤判的觀念。

8. **適合放入文章的證據**
  - 優先使用程式碼、測試結果、Git diff、輸入輸出與評分表。

9. **尚未確認的問題**
  - 統一標記為「待查證」，不可自行補完。

10. **延伸到下一篇的問題**
  - 指出今天實驗尚未處理的缺口，不使用空泛預告。

---

## 實驗設計與證據標準

### 每篇必須提出假設

範例：

```text
在固定 20 組 Issue 樣本中，
加入 JSON Schema 是否能提高 Java 解析成功率？
```

不合格的題目：

```text
Structured Outputs 是什麼？
```

### 一次只改一個主要變因

實驗應盡量固定：

- 測試資料。
- 評分標準。
- 執行流程。
- 程式版本。
- 人工審查規則。

單次實驗只改：

- Prompt。
- Schema。
- Tool Description。
- 模型或設定。
- Codex 任務限制。
- 錯誤處理策略。

若同時改變多個條件，必須說明無法單獨歸因。

### 每篇至少保留四類證據

1. **輸入**：Prompt、Issue、程式碼、文件或錯誤訊息。
2. **輸出**：AI 回答、JSON、工具提案、修改內容或執行結果。
3. **驗證**：Schema、JUnit、人工評分、Git diff 或官方文件核對。
4. **結論**：成功條件、失敗原因、限制與下一步。

### 統一實驗紀錄

`實驗/DayNN/README.md` 至少包含：

```text
核心問題：
假設：
實驗日期：
使用產品：
使用介面：
模型識別資訊：
SDK／工具版本：
Prompt 版本：
測試資料版本：
執行次數：
固定條件：
主要變因：
成功判定：
實際結果：
失敗案例：
人工修改：
已知限制：
待查證：
```

### 不得只展示成功案例

- 固定資料集不得因結果不好而臨時刪除。
- Prompt 或程式升級後應重新跑既有案例。
- 失敗輸出應保留，但公開前須先去除敏感資料。
- 無法重現的結果不得寫成一般結論。
- 單一成功案例只能證明「本次成功」，不能證明「普遍可靠」。

---

## 可量化成果指標

至少選擇與當篇問題相符的指標：

| 指標 | 計算方式 |
|---|---|
| Schema 通過率 | 符合 Schema 的輸出數／總輸出數 |
| Issue 完整度 | 依固定 Rubric 計算必填資訊平均分數 |
| 無依據內容率 | 無法由輸入或來源支持的陳述數／總陳述數 |
| 澄清問題品質 | 追問是否能有效補足缺失資訊 |
| 工具選擇正確率 | 選對工具且參數合理的案例數／工具案例總數 |
| 超出修改範圍率 | Codex 修改未允許檔案的任務數／總任務數 |
| Patch 接受率 | 人工 Review 後接受的修改數／總修改數 |
| 測試有效率 | Bug 存在時失敗、修復後通過的測試比例 |
| 敏感資料攔截率 | 成功遮罩案例數／敏感資料測試總數 |
| 人工介入次數 | 每次完整流程的人工確認次數 |
| 回歸失敗率 | 新版本造成既有案例失敗的比例 |
| 延遲與資源用量 | 每次請求的執行時間、用量與錯誤次數 |

若文章涉及 API 價格，必須查閱發布當日官方價格，不得把舊價格寫成現況。

---

## AI 安全、資料隱私與人工核准

### 輸入前檢查

每次將資料提供給 ChatGPT、OpenAI API、Codex 或第三方服務前，至少檢查：

```text
□ 是否含 API Key、Token、密碼或 Cookie？
□ 是否含真實姓名、電話、Email、地址或識別碼？
□ 是否含客戶資料、交易資料或健康資料？
□ 是否含公司未公開程式碼或商業機密？
□ 是否含內部網址、IP、主機名稱或帳號？
□ 是否含授權不明的第三方程式碼或文件？
□ 是否具備將資料提供給服務商的權限？
□ 是否可以使用合成資料或最小片段取代？
□ 是否真的需要上傳完整檔案或 Repository？
□ 是否已確認本次產品、方案與 API 的資料政策？
```

### Tool Calling 與執行規則

- 模型只能提出工具呼叫，不得直接視為已核准。
- 工具必須登錄於白名單，並定義輸入 Schema、權限、風險與可觀測紀錄。
- 所有參數在執行前需經程式驗證。
- 有副作用的操作必須經人工核准。
- 核准結果由系統或人員產生，模型不得偽造。
- 預設採最小權限、最小資料與最小執行範圍。
- 工具錯誤不得自動轉換成成功敘述。

### Codex 任務規則

每個 Codex 任務必須明確寫出：

```text
任務目標：
允許修改範圍：
禁止修改範圍：
允許執行的命令：
禁止執行的命令：
是否允許網路：
必須執行的測試：
驗收條件：
回報格式：
```

Codex 修改後至少檢查：

- 修改檔案是否超出範圍。
- 是否刪除、跳過或弱化測試。
- 是否新增未經核准的依賴。
- 是否更動安全設定或公開 API。
- 是否留下金鑰、個資或內部資訊。
- 測試是否真的能在 Bug 存在時失敗。
- Git diff 是否與任務目標一致。
- 是否需要人工拒絕整份修改。

---

## 三角色審查流程（每篇撰寫完、提交前必經）

**每寫完一篇文章，提交（git commit）前必須經過以下三個獨立子代理審查**（用 Agent 工具各派一個 general-purpose 子代理，可並行）。收到三份報告後，逐條處理有疑慮的點，通過才提交。這是硬性流程，不可略過。

1. **著作權合規審查**：確認**沒有逐字引用、也沒有貼近翻譯** ISO／CNS 等受著作權文件（**即使是翻譯也算抄襲**）。子代理須以 `參考/` 內對應正本逐句比對（正本多為 PDF，指示子代理用 scratchpad 的 venv ＋ pypdf 抽取文字比對），逐句判斷是「自己的話轉述」還是「貼近原文／翻譯」，標出風險等級與改寫建議。章節編號與標題、官方術語單詞屬事實，允許使用。
2. **事實正確性審查**：核對所有事實陳述（日期、條號、機關名稱、標題、採用關係、時序、概念主張）是否正確、轉述有無失真。同樣以 `參考/` 正本與可靠來源（必要時 WebSearch）佐證。**「換句話說也要說得正確」**——轉述不能扭曲原意。
3. **讀者清晰度與規範審查（總編輯）**：檢查讀者友善度（程式小白讀得懂、無跳步）、論文式名詞規範（首次全名、二次縮寫）、語氣（論文／出版書籍，非 AI／部落格腔）、標題層次（#→##→###、可直接發布、無「起承轉合」字樣）、起承轉合邏輯、**標點一致（全形中文標點）**、圖片規範（≥4–5 張、raw 網址、獨立成行）。

處理原則：著作權疑慮一律修到零風險；事實錯誤或術語不精確必改（優先採 `參考/` 正本的官方繁中術語）；編輯建議視情況採納，硬傷（錯字、破引用、名詞規範、標點不一致）必改。

### 本系列追加審查項目

三個角色除原有職責外，還需檢查：

- **著作權合規審查**
  - OpenAI、Java、Spring、JUnit、Git 與第三方技術文件是否有過度引用。
  - AI 產生內容是否與來源高度相似。
  - 程式碼與圖片是否具備可公開使用的授權。
  - 是否誤將「教學用途」視為當然免責。

- **事實正確性審查**
  - 是否正確區分 ChatGPT、OpenAI API、Codex、模型、IDE／CLI 與第三方框架。
  - API、模型、參數、價格、限制與資料政策是否以發布當日官方資料為準。
  - 實驗結論是否由實際證據支持。
  - 法規轉述是否避免自行下合規結論。

- **總編輯審查**
  - 是否清楚呈現輸入、輸出、驗證與限制。
  - 程式碼是否可執行且文章本身可獨立理解。
  - 是否揭露 AI 參與程度。
  - 是否與前後篇形成合理銜接。
  - 是否避免「完全自動化」「取代工程師」等無證據措辭。

### 審查報告格式

每個角色在 `審查/DayNN/` 產生獨立報告：

```text
審查角色：
審查日期：
審查範圍：
重大問題：
中度問題：
輕微問題：
需查證事項：
建議修改：
結論：通過／修改後通過／不通過
```

文章作者完成修正後，另外保存：

```text
問題：
處理方式：
修改位置：
是否接受建議：
不接受原因：
最終狀態：
```

三份審查未完成或仍有重大問題時，不得提交。

---

## 讀者定位與寫作風格（每篇一體適用）

本系列公開發表，主要讀者是具備一般軟體開發經驗的工程師，也包含技術主管、專案經理與希望理解企業 AI 工作流的非 AI 專家。

落筆前應確認：沒有 ChatGPT、Codex、Agent 或 Function Calling 經驗的 Java 工程師，是否仍能理解本文的問題、操作與結論。

- **逐步講清楚**：每個概念從問題與情境開始，不假設讀者已熟悉 AI 術語。
- **工程證據優先**：少用形容詞，多展示輸入、輸出、程式、測試、Git diff 與評分。
- **論文式名詞規範**：專有名詞第一次出現寫中文全名、英文全名與縮寫；第二次起才使用縮寫。
- **標題有實質意義**：不可使用「起、承、轉、合」作為標題文字。
- **標題層次固定**：`#` 文章大標 → `##` 大段 → `###` 子段，不跳級。
- **語氣專業、克制**：採技術書籍與工程紀錄風格，不使用浮誇 AI 宣傳語。
- **不掩飾失敗**：失敗實驗、錯誤輸出與人工修正是文章的重要內容。
- **有限結論**：只對實際測試的資料、版本與條件下結論。
- **法規表述謹慎**：區分條文內容、技術解讀、作者推論與待專業確認事項。
- **前後篇銜接**：每篇承接前一篇產出，並指出下一篇要解決的具體缺口。

禁止使用：

- 「AI 完全理解需求」。
- 「Agent 可以完全自動完成」。
- 「使用 RAG 就不會幻覺」。
- 「測試通過代表程式完全正確」。
- 「AI 產生內容一定不侵權」。
- 「Codex 已可取代工程師」。
- 未經量化或實驗支持的「大幅提升」「零錯誤」「百分之百可靠」。

---

## 每篇文章固定結構

每篇建議依下列順序撰寫：

1. 今天遇到什麼問題。
2. 為什麼值得解決。
3. 我原本的理解。
4. 查閱資料後學到的概念。
5. 本次實驗問題與假設。
6. 實驗環境、產品、版本與日期。
7. 操作步驟。
8. 程式碼或 Prompt。
9. 實際輸入與輸出。
10. 驗證方法。
11. 遇到的錯誤與修正。
12. 結果是否符合預期。
13. 本日結論。
14. 已知限制與待查證事項。
15. 下一篇要處理的問題。
16. 參考資料與查閱日期。
17. AI 參與程度揭露。

每篇至少應產生一項可展示成果：

- 一段可執行程式碼。
- 一個 Prompt 或 Schema。
- 一份測試結果。
- 一個 REST API。
- 一次 Codex 任務紀錄。
- 一張架構圖。
- 一份修改前後 Git diff。
- 一份評分或風險檢查表。

### AI 參與程度揭露範例

```text
本文的題目整理、部分初稿與測試案例由 ChatGPT 協助產生。
程式碼由作者人工檢查並執行測試。
Codex 產生的修改均透過 Git diff 與 JUnit 驗證。
產品功能、API 行為與法規內容以官方資料為準，查閱日期為 YYYY-MM-DD。
```

---

## 程式碼與實作 Demo 慣例

### 技術棧

- Java 21，或作者實際使用的 LTS 版本。
- Spring Boot 3.x；發布前記錄精確版本。
- Maven 或 Gradle，系列內盡量維持一致。
- Jackson。
- Jakarta Bean Validation。
- JUnit 5。
- Mockito、WireMock 或 MockWebServer，依實驗需要選用。
- Git。
- 前期可使用 H2，整合階段再評估 PostgreSQL。
- 前期優先直接呼叫 HTTP API；理解底層行為後，再比較 Spring AI 或 LangChain4j。

### 程式碼必須完整可執行

- 文章中的主要程式不得只放零碎片段。
- 不可用 `...`、`略` 或偽程式碼省略關鍵邏輯。
- 程式可依邏輯拆成多個區塊，但所有必要部分都要依序出現在文章中。
- 每個程式碼區塊後緊接說明：做什麼、為什麼這樣做、失敗時如何處理。
- 完整程式同時存放於 `程式碼/DayNN/`，文章本身仍需可獨立理解。
- 第二支高度相似的程式可只呈現差異，但需明確指出共用部分。
- 每個 Demo 都需提供啟動方式、必要環境變數、測試命令與預期結果。

### 安全規則

- 不在程式碼、文章、截圖、Log 或測試輸出中出現真實金鑰。
- 範例使用環境變數或 Secret 管理方式。
- Log 不記錄完整 Prompt、個資或敏感文件。
- 對外連線、Shell、檔案寫入與 Git 操作需明確說明權限。
- 涉及付費 API 時設定呼叫上限、逾時與錯誤處理。
- 不執行來源不明的模型輸出、Shell 指令或下載內容。

### 測試規則

- 新功能至少包含正常、邊界與失敗案例。
- AI 產生的測試需人工檢查。
- 驗證修正時，應先證明測試能在 Bug 存在時失敗，再證明修正後通過。
- 不得藉由刪除 assertion、停用測試或放寬條件使測試變綠。
- 重要 API 優先保留可離線執行的 Fake／Stub 測試。
- 真實 API 整合測試與單元測試分開，避免每次建置都產生成本。

---

## Prompt、結構化輸出與工具規範

### Prompt 版本控制

每一版 Prompt 都要記錄：

```text
名稱：
版本：
用途：
輸入欄位：
限制：
輸出格式：
相較前版變更：
測試資料版本：
評估結果：
```

Prompt 不只保存最終成功版，也保留失敗版與修改理由。

### 結構化輸出

- JSON 合法不代表內容正確。
- Schema 驗證、Java DTO 驗證與業務規則驗證需分層處理。
- 資料不足時應輸出 `unknownFields`、`assumptions` 或 `questionsToAsk`，不得自行虛構。
- 不以 Regex、字串截取或手動補括號假裝解析成功。
- 發生拒絕、截斷、逾時或格式錯誤時，需進入明確錯誤流程。
- 重試必須設上限，並記錄重試原因與結果。

### RAG

- 使用自製、公開授權或可公開的測試文件。
- 回應需附文件 ID、段落或可追溯來源。
- 檢索到文件不代表回答必然正確。
- 未檢索到證據時，模型應明確標記未知。
- Prompt Injection 測試文件需視為不可信輸入，不得讓文件內容改寫系統權限。

### Tool Calling

- Tool Description 應清楚說明用途、限制與禁止情況。
- 工具參數需經 Schema 與業務規則驗證。
- 模型只負責提出工具與參數，應用程式負責授權與執行。
- 有副作用的工具一律需要人工核准。
- 工具執行結果需原樣保存，模型摘要不可取代原始結果。

---

## 文章配圖

### 數量與用途

- 每篇原則上使用 **2–4 張**真正有助理解的圖片；複雜整合篇可增加。
- 至少包含一種工程證據型圖片，例如架構圖、流程圖、Git diff、測試結果、評分圖表或狀態機。
- 不為達到數量而加入無資訊價值的裝飾圖。
- 截圖中的金鑰、個資、內部網址與帳號必須遮蔽。
- 圖表資料應可回溯到 `實驗/DayNN/`。

### 命名與存放

- 放在 `圖檔/DayNN/`。
- 檔名：`DayNN-序號-英文簡述.png`。
- 序號兩位，由 `01` 起。
- 優先使用 16:9 橫向比例。
- 圖片需附繁體中文 alt 文字。
- 圖片在 Markdown 中獨立成行，前後留空行。

### 圖片來源

優先順序：

1. Mermaid、PlantUML 或自行繪製的架構圖。
2. matplotlib 產生的實驗資料圖表。
3. 自己截取並去識別化的執行結果。
4. AI 生成的原創概念圖。
5. 具備明確授權的第三方圖片。

使用 AI 圖像模型時：

- 發布前重新確認模型、API、尺寸與使用限制。
- 模型名稱、尺寸與參數不要寫死在規範中。
- 金鑰一律由環境變數讀取。
- 圖中文字盡量精簡，以繁體中文為主。
- 不生成近似受保護角色、品牌視覺或他人作品風格的圖片。
- 保留生成 Prompt、日期與人工修改紀錄。

若文章需要貼到 iThome，圖片可使用公開 Repository 的 raw 絕對網址；Repository 尚未確定前，不在範本中寫死帳號與專案名稱。

---

## 文章發布前檢查表

### 問題與內容

```text
□ 標題是否對應一個明確工程問題？
□ 是否說明為什麼值得解決？
□ 是否避免只介紹名詞？
□ 範圍是否能在單篇內完整回答？
□ 是否與核心專案有關？
```

### 實驗與證據

```text
□ 是否有實際輸入與輸出？
□ 是否有可重現操作步驟？
□ 是否記錄產品、模型、工具、版本與日期？
□ 是否有明確成功或失敗判定？
□ 是否包含測試、Git diff、Schema 或評分結果？
□ 是否保留失敗案例？
□ 是否說明人工修改與核准內容？
```

### 事實與查證

```text
□ OpenAI 產品功能是否查閱當日官方文件？
□ API、模型、價格、參數與限制是否重新確認？
□ 是否區分 ChatGPT、API、Codex、模型與第三方框架？
□ 是否避免把模型回答當成官方規格？
□ 動態資訊是否標示查閱日期？
□ 無法確認的內容是否標記「待查證」？
□ 法規內容是否引用官方來源？
□ 是否避免自行下法律結論？
```

### 著作權與引用

```text
□ 是否清楚標示來源與授權？
□ 是否避免大段複製或貼近翻譯官方與第三方文件？
□ 是否以自己的實驗、分析、程式與圖表為主？
□ 是否有權使用文中的程式碼、資料與圖片？
□ AI 產生內容是否經過人工檢查與相似性審查？
□ 是否避免宣稱 AI 輸出必然不侵權？
```

### 安全與隱私

```text
□ 是否移除 API Key、Token、密碼與 Cookie？
□ 是否移除真實個資？
□ 是否移除公司內部網址、IP、帳號與程式碼？
□ 是否優先使用自製或合成案例？
□ 是否說明資料送到哪一種產品、API 或第三方服務？
□ 是否限制工具權限與執行範圍？
□ 有副作用的操作是否經人工核准？
```

### 系列一致性

```text
□ 是否延續前一篇成果？
□ 是否為下一篇留下明確問題？
□ 是否沒有為了篇數過度拆分？
□ 是否產出至少一項可展示成果？
□ 是否揭露 AI 參與程度？
□ 結論是否只涵蓋實驗真正證明的範圍？
□ 三角色審查是否全部通過？
```

---

## Git 與提交規範

- 每天文章、程式、實驗與圖片盡量放在同一個可追溯 Commit。
- Commit 前必須執行當日相關測試。
- Commit 前必須完成三角色審查與修正。
- Commit Message 建議：
  - `docs(day01): define issue baseline`
  - `feat(day11): add OpenAI API client`
  - `test(day21): evaluate generated JUnit tests`
  - `fix(day23): correct stack trace analysis example`
- 不提交：
  - API Key、Token、Cookie。
  - 真實公司程式碼與客戶資料。
  - 受著作權保護且無權公開散布的 PDF、標準、圖片與表格。
  - 大型 Build 產物與 IDE 暫存檔。
- 發布後若修正事實、法規或安全問題，需在文章與 Git 歷史中保留更正紀錄。

---

## 最終成果驗收

Day 30 前應完成：

```text
□ 一個公開且不含敏感資料的 Java Repository
□ 一個可執行的 Spring Boot 應用程式
□ Issue 分析 REST API
□ Stack Trace 診斷 REST API
□ Issue 與診斷 JSON Schema
□ Prompt 版本紀錄
□ 最小 RAG 實驗
□ Tool Calling 白名單
□ 人工核准狀態流程
□ Codex 任務模板
□ 多組 Codex 修改前後 Git diff
□ JUnit 驗證結果
□ 固定評估資料集
□ 成功率、失敗率與人工介入統計
□ 資料隱私、著作權與法規查證清單
□ 完整失敗案例與限制清單
```

最終結論應採有限表述：

> 在本系列固定測試案例、工具權限與人工核准條件下，AI 適合協助整理資訊、產生草稿、提出假設與執行受限制的程式任務；涉及事實確認、優先級決策、敏感資料、程式碼合併與正式執行時，仍需要程式驗證與人工核准。

不得宣稱：

- AI 已完全取代工程師。
- Agent 可無人監督地處理正式系統。
- 通過測試即代表修改完全正確。
- 使用 RAG 即不會產生錯誤資訊。
- AI 產生內容必然沒有著作權問題。
- 單次成功 Demo 足以證明可投入企業正式環境。
