# DAY10 三角色審查與作者驗證清單

審查對象：`DAY10-Codex是什麼.md`

審查日期：2026-08-08

## 三角色終審結果

| 審查角色 | 第一輪主要疑慮 | 處理結果 | 終審 |
|---|---|---|---|
| 著作權合規 | 初稿沿用草稿「AI 建議」中的 `PriceCalculator`、六階段結構與近似句型 | 改用獨立的 `CsvBatchPlanner` 案例、四個檢查點與自行組織的權限說明；重生五張圖片並逐張檢查 | 通過，無風險 |
| 事實正確性 | 初稿把建置外掛寫成測試外掛；修正前後 `pom.xml` 的 artifactId 不同；核准策略與審查者角色混在同一句 | 依實際輸出改為 `maven-clean-plugin`；同步兩份 `pom.xml`；分開說明沙箱、核准策略與審查者 | 通過，無剩餘必改 |
| 讀者清晰度與規範 | 核心術語沒有正式帶出；舊 Maven Surefire 連結失效；驗證紀錄曾留有待填文字 | 補上代理迴圈、沙箱與核准策略；更新官方連結；填入修正前後實測結果 | 通過 |

## 著作權複核

- 本文未使用 ISO／CNS 第一層素材，不需比對 `條文/` 正本。
- 已逐段比對 `草稿/DAY10-simple.md` 的「AI 建議」，成稿未沿用其特徵性表達或示例。
- OpenAI、JUnit 與 Maven 內容只做短篇幅事實轉述並附官方來源。
- 五張圖片由本專案的 Pillow 腳本產生，沒有使用第三方照片、圖示或文件截圖。
- 程式差異、提示詞與測試輸出均為本案例自行產製或實測所得。

## 程式與事實證據

修正前在 `程式碼/DAY10/before-fix/` 執行：

```shell
mvn clean test
```

結果：`Tests run: 5, Failures: 2, Errors: 0, Skipped: 0`，`BUILD FAILURE`。0 筆被算成 1 批，1,000 筆被算成 3 批。

修正後在 `程式碼/DAY10/` 執行相同命令，結果為 `Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。

- 修正前後的 `pom.xml` SHA-256 相同。
- 修正前後的 `CsvBatchPlannerTest.java` SHA-256 相同。
- 主程式只調整 `countBatches` 的批次計數方式。
- 第一次受限執行在下載 `maven-clean-plugin:2.5` 時出現 `Permission denied: connect`，當時尚未執行 JUnit 5。

## 格式檢查

- 正文 CJK 統一漢字數：1,069。
- 圖片：5 張，皆為 1280×720、16:9，且每張使用獨立一行的 raw URL。
- 標題：單一 `#`，其餘使用 `##`。
- 已首次寫出代理迴圈（agent loop）、沙箱（sandbox）與程式碼差異（diff）。
- 未發現「AI 建議」「待填」「待確認」「TODO」等骨架文字。
- Day 09 開場、Day 02 回扣與 Day 11 預告均已銜接。

## 發布前作者親自確認

- [ ] 以自己的實際操作經驗確認全文第一人稱敘述，再做最後改寫，不直接照搬成稿。
- [ ] 在發布當日重新核對 OpenAI Codex、沙箱與核准文件，避免介面或行為更新造成落差。
- [ ] 確認 `程式碼/DAY10/`、`圖檔/Day10/` 與本文已推送到公開 GitHub `main`。
- [ ] 用未登入瀏覽器開啟五張 raw 圖片、三個 DAY10 GitHub 連結與全部參考資料。
- [ ] 檢查文章與圖片沒有信箱、帳號、路徑中的個資、權杖或組織資訊。
- [ ] iThome 發布後若需修改，於發布當日完成。
