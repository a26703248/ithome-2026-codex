# DAY09 三角色審查與發布前驗證

審查對象：`DAY09-用ChatGPT做CodeReview初篩.md`

審查日期：2026-08-08；補作對話稿複審：2026-08-22

## 審查結論

| 審查角色 | 初審主要疑慮 | 處理結果 | 最終複核 |
|---|---|---|---|
| 著作權合規 | 初稿有數處措辭與草稿「AI 建議」接近，README 與第二張圖也沿用相似判斷順序 | 重寫開場、判斷標準、提示詞格式、結尾、README 與圖片文字；再次比對全文與產物 | 通過，無剩餘阻擋 |
| 事實正確性 | 修正前 4 項測試中 3 項失敗只有文字紀錄，第三方無法獨立重現；未保存真實 ChatGPT 回覆卻寫成實測 | 新增 `before-review/` 獨立失敗夾具；正文、表格、筆記與圖片明示為刻意設計的模擬初篩，不代表真實對話 | 通過，修正前後結果均實跑核對 |
| 讀者清晰度與規範 | 名詞未先給全名、並行測試說明跳步、Day 08 銜接與演練假設不清楚、圖片行號及用詞不一致 | 補全名與白話時序、標明非 v0 產品決策、交代審查範圍轉換；重做第三、第五張圖並同步全文 | 通過，無剩餘阻擋 |

### 2026-08-22 補作對話稿複審

| 審查角色 | 本輪主要疑慮 | 處理結果 | 最終複核 |
|---|---|---|---|
| 著作權合規 | 連結文字與附件標題可能使讀者誤認為原始對話；附件包含模擬 AI 回覆 | 正文改稱「完整補作對話稿」；附件明示為本次編修建立的模擬稿，不是逐字復原或 ChatGPT 產品匯出 | 通過，風險低 |
| 事實正確性 | 第二個 `Set.add` 的回傳值敘述不精確；正文把 `VERIFY` 待查項目稱為誤報 | 明寫 A 的 `add` 回傳 `true`、B 的 `add` 回傳 `false`，但兩個 `tryStart` 都回傳 `true`；全文統一為「待查後排除」 | 通過，無剩餘阻擋 |
| 讀者清晰度與規範 | `newKeySet` 分類與補作回覆矛盾；API 首次出現未展開全名 | 同步正文、附件、筆記與第五張圖；補上應用程式介面（Application Programming Interface，API）全名 | 通過，無剩餘硬傷 |

## 著作權與引用檢查

- 本篇未使用 ISO／CNS、法律條文或 CC 授權素材，不需讀取或比對 `條文/` 正本。
- OpenAI、Oracle、JUnit 與 Apache Maven 文件只用來核對產品定位與 API 事實；正文沒有逐段翻譯或大量引用。
- 草稿中的「AI 建議」未直接沿用；開場、判斷方法、提示詞輸出格式與小結均已重新設計。
- Java 程式、JUnit 5 測試、提示詞與模擬複核筆記為本案例原創產物。
- 五張圖片由 `圖檔/Day09/generate_day09_images.py` 從空白畫布產生，只使用文字、幾何圖形與本案例程式片段，未含第三方圖片或標誌。

## 事實與程式核對

- OpenAI 官方目前將對話探索與 Codex 的程式碼庫理解、測試及審查工作分開描述；正文只做概括轉述。
- `ConcurrentHashMap.newKeySet()` 產生由 `ConcurrentHashMap` 支援的集合；把容器本身標成不安全是模擬誤報。
- `contains` 與 `add` 分成兩次呼叫時，兩個執行緒可能都先看到鍵不存在；修正版直接使用該集合單次 `add` 的回傳值判斷。
- 缺陷版對 null 呼叫 `isBlank()`，實際會先拋出 `NullPointerException`。
- `tryStart` 與 `finish` 共用 `normalize` 後，前後空白與英文字母大小寫的識別規則一致。
- `before-review/` 額外加入 package-private 的 `Set<String>` 建構子，僅用來注入測試集合並固定時序；業務方法仍保留待審 diff 的三個缺陷。
- `BarrierSet.add` 讓兩個呼叫都抵達 `CyclicBarrier` 後，再進入底層集合；它控制程式呼叫順序，不宣稱 CPU 指令同時執行。
- 匯入閘門只保護一份 Java 服務；多份服務仍需資料庫或其他共用協調機制。
- `chatgpt-review-record.md` 是依既有提示詞與 diff 建立的補作模擬對話稿，不是遺失原始對話的復原，也不是 ChatGPT 產品介面的匯出紀錄。
- 補作稿中的並行時序已明寫：A 的 `Set.add` 回傳 `true`，B 的 `Set.add` 回傳 `false`；缺陷版忽略回傳值，所以兩個 `tryStart` 都回傳 `true`。

## 測試結果

在 `程式碼/DAY09/before-review/` 執行：

```shell
mvn clean test
```

結果為 `Tests run: 4, Failures: 3, Errors: 0, Skipped: 0`，`BUILD FAILURE`。三個失敗分別是 null 例外型別、兩次並行啟動皆成功，以及完成後未釋放正規化鍵；此失敗是刻意保留的紅燈基準。

在 `程式碼/DAY09/` 執行：

```shell
mvn clean test
```

結果為 `Tests run: 4, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。

## 格式與產物檢查

- 正文排除 fenced code、圖片行、參考資料與網址後，共 987 個漢字及 90 個全形標點，合計 1,077 字，落在 900～1,100 字目標，也超過 300 字下限。
- 標題首次使用「程式碼審查（Code Review）」；API、JDK 也在首次出現時提供中英文全名。
- 標題層級為一個 `#` 主標題，後續使用 `##`，沒有跳級。
- 五張圖片皆為 1280 × 720 px、16:9；文章使用 `raw.githubusercontent.com` 網址，且每張圖片獨立成行。
- 第三張圖的程式行號 13、16、17、20、25 與 `review-input.diff` 一致，第 16 行完整呈現 `toLowerCase(Locale.ROOT)`。
- 正文、補作稿、比較表、`review-notes.md` 與第五張圖均標明模擬性質，不冒充作者遺失的 ChatGPT 原始對話紀錄。
- Day 08 的時區計算已有測試，本篇明確把審查範圍移到排程外新增的匯入閘門；小結再銜接 Day 10 的 Codex 代理任務。

## 作者發布前確認

- [ ] 親自閱讀全文與補作模擬對話稿；若之後取得真實 ChatGPT 實測紀錄，須同步替換正文、附件、表格、筆記與第五張圖後重新審查。
- [ ] 親自重跑修正前與修正版 Maven 指令，確認本機仍分別得到 3 項失敗與 0 項失敗。
- [ ] 確認「同一工作區同時只能有一批匯入」與識別字正規化仍清楚標為本文演練假設，不是 v0 需求書的正式決策。
- [ ] 圖片、程式與本文推送至 GitHub `main` 後，逐一點開五個 raw 圖片網址及所有程式連結。
- [ ] 上線當日重新確認 OpenAI、Oracle、JUnit 與 Maven 官方參考連結仍可存取。
- [ ] 發布後若需修改，於發布當日完成。
