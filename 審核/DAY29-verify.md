# Day 28 發布前驗證

驗證日期：2026-08-10

## 文章與素材

- 主文：`DAY28-用數據說話.md`。
- 依「中文字＋常用全形標點」計算：全文（含標題、表格、程式碼、圖說、參考資料）約 1,681 字；純正文（不含標題／表格／程式碼／圖片／參考資料）約 1,125 字，略高於 900～1,100 字目標區間上緣（超出約 25 字），仍遠超競賽 300 字下限；已嘗試精簡用字，考量再刪會犧牲論證完整性，予以保留，作者可於發布前視情況再微調。
- 內容承接 Day 27 結尾預告，處理 Day 24～29 共用的 `案例/AI輔助生成系統-需求書.md` 第七節「如何量測是否真的縮短撰寫時間、草稿被大幅修改或否決的比例」，並誠實說明 Day 01 基準線卡片為何不能直接拿來做速度比較，預告 Day 29 主題。
- 量測工具（`ReviewMetricsCalculator` 及相關類別）、測試、配對前後測與批次比較的計時紀錄位於 `程式碼/DAY28/`。
- 五張圖片位於 `圖檔/Day28/`，皆為 1600×900、16:9；主文使用 raw GitHub 網址，且每張獨立成行。
- 圖片由 `generate_day28_images.py` 產生，沒有匯入外部照片、圖示、標誌或官方介面截圖；沿用 Day 24～27 腳本的配色與字型偵測邏輯（Microsoft JhengHei／Noto Sans CJK TC 自動偵測）。

## 可重現驗證與邊界

- `ReviewMetricsCalculator.calculate()` 把一批 `ReviewRecord` 彙總成生成到定稿時間、複核時間、大幅修改或否決比例、任務完成率、平均引用錯誤數五項指標；傳入 `null` 或空清單皆丟出例外，不會除以零算出誤導性平均值。
- `ReviewMetricsCalculatorTest` 共 8 項測試，依程式邏輯與手算覆核設計正確，包含 null／空清單例外、單筆與多筆平均、比例定義區分（大幅修改或否決比例 vs 任務完成率）、以及套用 `docs/baseline-log.md` 16 筆樣本資料的彙總結果比對。本次草擬環境只安裝 OpenJDK 11 的 JRE（缺 `javac`）、沒有 Maven，且沙盒對外網路受限、無法下載 Maven 或 JUnit 相依套件，因此**尚未實際編譯或執行 `mvn clean test`**。這一點與 Day 24／25 已附上真實建置輸出不同，已在 `程式碼/DAY28/docs/verification-log.md` 與 `README.md` 中明確標示為待作者本機驗證事項，不宣稱已通過測試。
- `程式碼/DAY28/docs/baseline-log.md` 的配對前後測分鐘數、返工原因，以及基準線／AI 協作各 8 筆的批次比較數字，是本次撰寫過程與案例情境設計的示範記錄，不是自動化量測工具產出，也不是正式系統上線後的統計結果。
- 本篇沒有呼叫正式模型或連線正式系統，所有情境皆為依需求書改編的桌上演練。

## 三角色審查

- **著作權合規**：通過，必改 0。專案內無 `條文/` 正本資料夾，本篇不涉及 ISO／CNS 或法律條文引用；對需求書第七節僅為短句轉述並加註出處；`ReviewMetricsCalculator` 等 Java 程式碼為原創聚合邏輯，非抄襲特定專案；圖片由 `generate_day28_images.py` 純程式繪製，無外部素材；對 Day 01、Day 27 的引用屬改寫呼應，非逐段複製。
- **事實正確性**：通過，必改 0。主文表格、程式碼片段與 `docs/baseline-log.md`、`ReviewMetricsCalculatorTest` 逐項核對一致（配對前後測 48 分鐘／26 分鐘及返工原因、批次比較五項指標數字與差異換算）；「8 項測試」與測試檔實際數量相符；未使用「測試已通過（執行）」等可能誤導字句；對 Day 01 基準線卡片、Day 27 結尾預告的描述與原文相符，時序正確；需求書第七節引用文字準確。低風險編輯建議「約 4 倍」改為「基準線的 4 倍」已採納。
- **總編輯（讀者清晰度與規範）**：通過，必改 0。語氣、標題層次、全形標點、圖片規範（5 張、raw 網址、獨立成行、16:9）與 Day 27／Day 29 前後銜接均通過；建議「四格決策規則」首次出現時補充說明其餘三象限，已採納並補寫於「分析」段落。
- 三份報告的建議事項已全部處理完畢；主文與 `程式碼/DAY28/docs/baseline-log.md` 無殘留錯誤歸因或未採納的必改項。
- 主文沒有 `AI 建議`、`作者撰寫區塊` 或 `待填` 等草稿標記。

## 作者發布前仍需確認

- [ ] 逐段改成作者確實做過、願意署名負責的第一人稱經驗；不可未經消化直接發布生成內容，包括 `docs/baseline-log.md` 的計時數字與批次樣本資料。
- [ ] 在本機（建議 Windows 11、OpenJDK 17、Apache Maven 3.8 以上）執行 `mvn clean test`，把真實建置結果回填 `程式碼/DAY28/docs/verification-log.md` 與 `README.md`，取代目前的人工推演說明。
- [ ] 確認團隊真實的複核流程、大幅修改或否決比例與引用錯誤標準，目前門檻與批次數字僅為示範內容，不是正式系統統計結果。
- [ ] 提交並推送主文、程式碼與五張圖片後，逐一開啟 GitHub 與 raw 圖片網址確認可正常顯示。
- [ ] 發布當日再次確認 Codex／ChatGPT 相關敘述、iThome 預覽、圖片顯示、字數與連結；需要修正時在當日內完成。
- [ ] 發布後不要刪除文章。
