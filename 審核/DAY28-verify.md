# Day 27 發布前驗證

驗證日期：2026-08-10

## 文章與素材

- 主文：`DAY27-成本與效能管理.md`。
- 依「中文字＋常用全形標點」計算：全文（含標題、表格、程式碼、圖說、參考資料）約 1,764 字；純正文（不含標題／表格／程式碼／圖片／參考資料）約 1,086 字，落在 900～1,100 字目標區間，並超過競賽 300 字下限。
- 內容承接 Day 26 提到的「補 OCR／ETL 前處理單元測試」任務，以 Day 24～29 共用的 `案例/AI輔助生成系統-需求書.md` 第七節（OCR 辨識準確率門檻）為主軸，並預告 Day 28 的量測主題。
- OCR 分流最小案例、測試、計時紀錄與驗證紀錄位於 `程式碼/DAY27/`。
- 五張圖片位於 `圖檔/Day27/`，皆為 1600×900、16:9；主文使用 raw GitHub 網址，且每張獨立成行。
- 圖片由 `generate_day27_images.py` 產生，沒有匯入外部照片、圖示、標誌或官方介面截圖；沿用 Day 24～26 腳本的配色與字型偵測邏輯（Microsoft JhengHei／Noto Sans CJK TC 自動偵測）。

## 可重現驗證與邊界

- `OcrPreprocessor.evaluate()` 依信心分數與重試次數，將 OCR 結果分流為 `ACCEPT`／`MANUAL_REVIEW`／`RETRY`／`REJECT`，門檻為本次案例示範設定，非公司已核定的正式標準。
- `OcrPreprocessorTest` 共 8 項測試，依程式邏輯人工覆核設計正確，涵蓋一般情境與信心分數剛好等於 0.90／0.70 的邊界情境。本次草擬環境只安裝 OpenJDK 11 的 JRE（缺 `javac`）、沒有 Maven，且沙盒對外網路受限、無法下載 Maven 或 JUnit 相依套件，因此**尚未實際編譯或執行 `mvn clean test`**。這一點與 Day 24／25 已附上真實建置輸出不同，已在 `程式碼/DAY27/docs/verification-log.md` 與 `README.md` 中明確標示為待作者本機驗證事項，不宣稱已通過測試。
- `程式碼/DAY27/docs/cost-log.md` 的分鐘數、對話輪次與策略 A／B 比較，是本次撰寫測試檔過程的人工計時紀錄，不是自動化量測工具產出，也不含實際模型方案費用或詞元（token）計價。
- 本篇沒有呼叫正式模型或連線正式系統，所有情境皆為依需求書改編的桌上演練。

## 三角色審查

- **著作權合規**：通過，剩餘必改 0。專案內無 `條文/` 正本資料夾，本篇不涉及 ISO／CNS 或法律條文引用；對需求書第七節僅為短句轉述加註出處，非逐字照搬；Java 程式碼為原創的門檻分流邏輯，非抄襲特定專案；Codex、ChatGPT 等名稱僅為事實敘述。
- **事實正確性**：初審發現 2 項必改，已修正——(1) 原文誤將「複核介面加否決原因欄位」與其相依性判斷歸功給 Day 26 治理盤點表，但 Day 26 正文與 `governance-inventory.md` 並未列出這項比較，已改寫為本篇作者這次自行比較，並同步修正 `程式碼/DAY27/docs/cost-log.md` 對應措辭；(2) 開場段落把「同事認領任務」與「盤點表列項」的時序寫反，已改為先有 Day 26 事件、後續才寫入盤點表的正確順序。核對通過項目：`cost-log.md` 各項數字與文章表格逐項一致；文章程式碼片段與 `OcrPreprocessor.java` 逐行一致；測試檔確為 8 項且邊界案例（0.90／0.70）對應正確；文章與 `verification-log.md` 皆誠實揭露尚未實際執行 `mvn test`，未使用「測試執行通過」等可能誤導字句。
- **總編輯（讀者清晰度與規範）**：初審發現 1 項必改——策略比較段落「9 個測試」與後段「8 項測試」之間的落差原先未說明原因，已補上「1 個斷言只檢查型別、修正後與另一邊界案例重複而整併」的說明。低風險建議：統一使用「詞元（token）」中英並列寫法（已採納，表格與內文均已更新）；OCR／ETL 全名沿用 Day 26 已展開版本，屬系列前後呼應的合理判斷，非必改。語氣、標題層次、全形標點、圖片規範（5 張、raw 網址、獨立成行）、與 Day 26／Day 28 的前後銜接均通過。
- 三份報告的必改項目已全部處理完畢；主文與 `程式碼/DAY27/docs/cost-log.md` 已同步更新，沒有殘留錯誤歸因。
- 主文沒有 `AI 建議`、`作者撰寫區塊` 或 `待填` 等草稿標記。

## 作者發布前仍需確認

- [ ] 逐段改成作者確實做過、願意署名負責的第一人稱經驗；不可未經消化直接發布生成內容，包括 `docs/cost-log.md` 的計時數字。
- [ ] 在本機（建議 Windows 11、OpenJDK 17、Apache Maven 3.8 以上）執行 `mvn clean test`，把真實建置結果回填 `程式碼/DAY27/docs/verification-log.md` 與 `README.md`，取代目前的人工推演說明。
- [ ] 確認團隊真實的 OCR 信心分數門檻、複核流程與訂閱方案／API 計價，目前門檻值與費用敘述僅為示範內容。
- [ ] 提交並推送主文、程式碼與五張圖片後，逐一開啟 GitHub 與 raw 圖片網址確認可正常顯示。
- [ ] 發布當日再次確認 Codex／ChatGPT 相關敘述、iThome 預覽、圖片顯示、字數與連結；需要修正時在當日內完成。
- [ ] 發布後不要刪除文章。
