# DAY03 發布前人工驗證與三角色審查紀錄

這份檔案是 `DAY03-AI-開發環境總覽.md` 的作者工作單，不是公開文章內容。2026 年 8 月 6 日已完成著作權、事實正確性、讀者清晰度與規範三角色審查；目前仍有必改項目與只能由作者確認的經驗，因此文章尚未通過發布或 `git commit` 條件。

## 目前狀態

- [x] 已完成 Day 03 文章、Java／Maven 案例、驗證紀錄及 4 張正文圖片。
- [x] 正文以 ChatGPT、Codex、最小環境與購物車案例串起操作流程，並銜接 Day 04。
- [x] 已完成三個獨立子代理的著作權、事實正確性及總編輯審查。
- [x] 4 張正文 PNG 均為 960 × 540 px，比例為 16:9，本機檢視未發現破字、裁切或溢出。
- [ ] 尚未處理三份審查報告的全部必改項目，因此不能標示為三角色審查通過。
- [ ] 作者須確認第一人稱操作經過與購物車案例來源，不得把草稿中標示為「AI 建議」的內容直接當作本人經驗。
- [ ] 作者須決定 Java 專案要保存修正前或修正後版本，並讓原始碼、文章與驗證紀錄一致。

## 量化與自動檢查

| 檢查項目 | 目前結果 | 判定 |
|---|---:|---|
| 全檔漢字數 | 1,058 | 符合 900～1,100 目標 |
| 排除圖片 Markdown 與參考資料後漢字數 | 1,004 | 符合 900～1,100 目標 |
| 標題層級 | 1 個 `#`、6 個 `##` | 通過，沒有跳級 |
| 正文圖片 | 4 張 | 符合 4～5 張要求 |
| 圖片尺寸 | 全部 960 × 540 px | 通過，16:9 |
| GitHub raw 圖片網址 | 0／4 | 不通過，目前皆為相對路徑 |
| 半形分號 | 2 處 | 不通過，應改為全形中文標點並拆句 |
| 2026-08-06 `mvn test` | 2 項測試、0 failures、1 error | 不通過，目前原始碼仍會 `/ by zero` |

## 作者必須確認的案例事實

- [ ] 2026 年 8 月 5 日確實先用 ChatGPT 確認預期行為，再由 Codex App 修改並審查專案。
- [ ] 購物車案例是作者原先就選定並實際操作的案例，而不是讀到 `草稿/DAY03-simple.md` 的「AI 建議」後才建立；若無法確認，應改用作者獨立選定的真實案例。
- [ ] 當次確實只修改 `ShoppingCart.calculateTotal()`，沒有更動測試、滿額折扣邏輯或夾帶無關重構。
- [ ] Maven 第一次受網路權限阻擋時，實際錯誤確實發生在下載 `maven-resources-plugin`，而且當時已由畫面或網址確認來源為 Maven Central。
- [ ] `verification-log.md` 的修正前與修正後數字來自當次實際輸出，不是事後依預期結果補寫。
- [ ] 圖片中的 Windows 11、OpenJDK 17.0.10、Maven 3.8.1、JUnit 5.10.2 與 Git 2.36.1 是作者當次實測環境；若要標示完整 Git 版本，應使用 `2.36.1.windows.1`。
- [ ] 5 份 SVG／PNG 為作者或本專案工具自行產生，未使用第三方照片、圖示、Logo 或未授權字型。
- [ ] 未被文章引用的 `codex-change.jpg`、`mvn_test.jpg`、`mvn_test_success.jpg` 不會公開；若之後使用，須先遮蔽本機使用者名稱、路徑及其他可能識別資訊。

## 三角色審查

### 著作權合規

- [x] 本文未涉及 ISO／CNS、法律條文或 OWASP 素材，不需比對 `條文/` 正本。
- [x] OpenAI 官方文件只用於功能事實的短篇幅轉述與列示來源，未發現逐句翻譯或大段近似原文。
- [x] 正文引用的 4 張圖只含基本圖形與本專案文字，未嵌入外部圖片或第三方 Logo。
- [ ] 開場的「ChatGPT → Codex → Maven」流程、工具比較、購物車案例與小結，和草稿中標示為「AI 建議」的段落有明顯承接，尚不符合 `AGENTS.md` 的零沿用要求。
- [ ] 「交代任務、修改、測試、看差異」與圖 4 的「提出任務 → 修改 → 測試 → 看差異」須改成作者依真實操作歸納的獨立表達。
- [ ] 作者須確認案例在 AI 建議出現前已存在；若沒有，改用獨立案例後重新進行本項審查。

目前判定：第三方著作權風險低；AI 建議沿用的專案規範風險為中，暫不通過。

### 事實正確性

- [x] 官方資料支持：ChatGPT Web 可用於研究、分析與檔案工作；OpenAI API 由程式呼叫，需自行管理金鑰與呼叫流程。
- [x] 官方資料支持：Codex CLI 可在終端機處理本機專案；Codex cloud 在隔離的雲端環境執行工作；Codex App 可管理專案、較長任務與檔案差異。
- [x] `pom.xml`、測試程式與環境資料可確認 JUnit 5.10.2，以及購物車案例共有 2 項測試。
- [x] 2026 年 8 月 6 日重新執行 `mvn test`，成功進入 JUnit 後重現 `ArithmeticException: / by zero`；結果為 2 項測試、0 failures、1 error、BUILD FAILURE。
- [ ] 正文「ChatGPT App 與 Web 功能上一模一樣」不正確；兩者有重疊能力，但桌面 App、Web、作業系統、方案、地區、工作區設定與功能推出進度可能造成差異。
- [ ] 正文「ChatGPT App 整合進 Codex」關係倒置；應改成 ChatGPT desktop app 內可在 ChatGPT 與 Codex 間切換。
- [ ] 目前 `ShoppingCart.java` 沒有 `items.isEmpty()` 防護，JavaDoc 也明確標示缺陷仍刻意保留；這和正文及 `verification-log.md` 宣稱已修正、2 項測試通過不一致。
- [ ] 正文寫「完整輸出放在驗證紀錄」，但該檔只保存關鍵輸出與摘要；應改稱「關鍵輸出與驗證摘要」，或另附完整原始 log。

Java 案例必須二選一後重新驗證：

1. 將空清單修正重新套回原始碼，更新 JavaDoc，執行 `mvn test` 並保存新的通過證據；或
2. 明確將目前專案標為「修正前起始版本」，另外保存修正後 patch／程式碼與對應測試紀錄，避免讓讀者誤認目前原始碼就是完成版本。

目前判定：產品敘述與 Java 專案狀態共有 3 類必改問題，暫不通過。

### 讀者清晰度與規範

- [x] 標題層級清楚，敘事順序為操作介面比較、最小環境、實測、驗證與 Day 04 銜接。
- [x] 正文包含 `ArithmeticException`、`mvn test`、網路權限與最小修改等具體細節，整體方向符合工程師實戰筆記。
- [x] 4 張正文圖均為 16:9，且本機檢視沒有破字、裁切、溢出或遮字。
- [x] 圖 4 的 SVG 與 PNG 都已將「AI 入口」改成「操作介面」。
- [ ] 4 張正文圖目前全是相對路徑，必須改成 GitHub raw 網址並保持每張獨立成行。
- [ ] 開場的「談完……後，但……」轉折衝突，且「今天我們要來探討」屬預告式寫法；應改成第一人稱實際動作與觀察。
- [ ] ChatGPT 比較段有「三種app操作方式功能上面相不同情境」等語病、中英文間距缺漏、半形分號與過長句，必須重寫。
- [ ] `API` 首次出現時須寫成「應用程式介面（Application Programming Interface，API）」。圖中出現 `IDE` 時，正文應先寫「整合開發環境（Integrated Development Environment，IDE）」。
- [ ] `Day03` 應統一為 `Day 03`；「以上是我的程式測試執行版本」應改為能涵蓋作業系統、Git 與工具的「以上是我這次的實測環境」。
- [ ] 圖 2 仍使用「ChatGPT 的入口」，若全文統一稱「操作介面」，應同步修改 SVG 與 PNG。

目前判定：結構、圖片尺寸與案例細節通過；raw 網址、語病、名詞與標點仍有硬傷，暫不通過。

## 發布前處理順序

1. 作者先確認案例來源與第一人稱操作經過，決定是否保留購物車案例。
2. 決定 Java 專案保存修正前或修正後狀態，讓原始碼、JavaDoc、文章與驗證紀錄一致，再重新執行測試。
3. 重寫開場、ChatGPT 比較段與小結，修正 App／Web／Codex 關係，並避免沿用草稿「AI 建議」的句型。
4. 修正首次全名、中文標點、中英文間距、`Day 03` 格式與「完整輸出」用語。
5. 將 4 張圖片改為 GitHub raw 網址；若統一「操作介面」用詞，同步重新輸出圖 2。
6. 作者逐段以自己的經驗與語氣完成個人化編修，確認直接引用與直接沿用 AI 產出合計低於全文三分之一。
7. 重新執行三角色複審；所有必改項目完成後，才可標記為通過並考慮 `git commit`。

## 發布前最後清單

- [ ] 中文字數含標點超過 300 字，且內容不是靠重複敘述湊字數。
- [ ] 作者已逐段消化並改成自己的說法，沒有把草稿「AI 建議」納入文章。
- [ ] 第一人稱經驗、版本、錯誤訊息、權限操作與測試結果均由作者本人確認。
- [ ] Java 原始碼、JavaDoc、文章、驗證紀錄與最新測試結果完全一致。
- [ ] 4 張 raw 圖片網址推送後都能載入，並在 iThome 編輯器及約 360～375 px 寬度下檢查可讀性。
- [ ] 發文當日重新開啟 OpenAI 官方參考連結，確認產品名稱、操作介面與可用性限制沒有更新。
- [ ] 直接引用及直接沿用 AI 產出合計低於全文三分之一。
- [ ] 三角色複審已通過，所有硬傷與疑慮均已處理。
- [ ] 若發布後需要修改，於發布當日內完成；完賽後不要刪除文章。

目前結論：人工審核檔已補齊，但 Day 03 尚未通過發布或提交條件。本次不執行 `git commit`。
