# DAY20 發布前驗證

## 內容與結構

- [x] 發布可見中文字與全形中文標點（排除圖片行）共 1,003 字，落在 900～1,100 字。
- [x] 承接 Day 19 的新需求 07:00 縮小案例，明確說明 Day 20 回到尚未套用新需求的既有 08:00 路徑。
- [x] 案例只使用 `案例/日報服務-需求書.md`，並銜接 Day 21 的延遲寄送除錯。
- [x] 共 5 張圖片，尺寸均為 1600 × 900（16:9）。
- [x] 圖片使用 `raw.githubusercontent.com` 網址，且每張圖片獨立成行。
- [x] 程式碼使用 Java 17、Maven 與 JUnit 5.13.4。

## 事實與實作

- [x] OpenAI「Understand large codebases」與 JUnit 5.13.4 文件已於 2026-08-09 核對。
- [x] 呼叫路徑已核對：`runAt()` → `findDailySubscribers()` → `generateAndSend()` → `loadPreviousDay()` → `render()` → `send()`。
- [x] `DailyReportJob` 使用注入的報表時區；只有特徵測試明確注入 `Asia/Taipei`。
- [x] `2026-08-19T00:00:00Z` 對應臺北同日 08:00。
- [x] 單次執行時，PDF 產生器與郵件閘道測試替身各被呼叫 1 次；同一分鐘執行兩次時各被呼叫 2 次。
- [x] 正文與圖片只宣稱測試替身被呼叫，未宣稱已產生真正 PDF、寫入檔案、寄出或送達郵件。
- [x] 最終執行 `mvn clean test`：2 項測試、0 失敗、0 錯誤、0 略過，結果為 `BUILD SUCCESS`。
- [x] 正式排程器、設定來源、資料庫、PDF 套件、郵件重試與送達結果均保留為未驗證。

## 三角色審查

- [x] 著作權合規複核通過；未涉及 ISO／CNS，未逐字沿用草稿「AI 建議」，外部來源沒有過度引用。
- [x] 事實正確性複核通過；時間邊界、時區、呼叫路徑、測試數字與證據範圍一致。
- [x] 讀者清晰度與規範複核通過；篇幅、名詞、全形標點、標題層級與五張圖片均符合規範。

## 作者發布前仍需確認

- [ ] 確認第一人稱操作與判斷符合作者親自實作後可負責的經驗，必要時改寫。
- [ ] 發布當日再次確認 OpenAI 官方文件與產品介面。
- [ ] 將 DAY20 文章、程式碼、驗證紀錄與 5 張圖片提交並推送至 GitHub `main`，再逐一點開 GitHub／raw 連結；推送前 DAY20 新連結會是 404。
- [ ] 發布後若需修改，於發布當日完成。
