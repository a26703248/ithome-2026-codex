# Day 24 發布前驗證

驗證日期：2026-08-09

## 文章與素材

- 主文：`DAY24-不只是工程師.md`。
- 依「中文字＋常用全形標點」計算為 1,084 字，符合 900～1,100 字目標，且超過競賽 300 字下限。
- 內容承接 Day 23 的證據鏈，使用 Day 24～29 共用的 `案例/AI輔助生成系統-需求書.md`，並預告 Day 25 的團隊提示詞資產。
- Java 最小案例、任務說明、交付紀錄與驗證紀錄位於 `程式碼/DAY24/`。
- 五張圖片位於 `圖檔/Day24/`，皆為 1600×900、16:9；主文使用 raw GitHub 網址，且每張獨立成行。
- 圖片由 `generate_day24_images.py` 產生，沒有匯入外部照片、圖示、標誌或官方介面截圖。

## 可重現驗證

- 環境：Windows 11、OpenJDK 17.0.10、Apache Maven 3.8.1、JUnit Jupiter 5.13.4。
- `mvn clean test`：4 項測試、0 項失敗、0 項錯誤、`BUILD SUCCESS`。
- 第一次在受限環境執行時，Maven 無法下載缺少的外掛，測試尚未開始；核准必要連線後重跑成功，沒有把建置環境問題誤記為測試失敗。
- 測試涵蓋採用、修改時新增版本、三種決策的複核原因必填，以及否決後保留原稿。
- 文章程式片段與 `ReviewWorkflow.java` 一致，先將 `null` 原因正規化，再檢查空白內容。
- 使用者介面、資料庫、正式登入授權、稽核儲存、版本衝突、敏感資料處理、監控與部署復原均明確列為待真實環境驗證。

## 三角色審查

- 著作權合規：第二輪通過，剩餘必改 0。本文沒有 ISO／CNS 或法律條文內容；OpenAI 官方頁只有標題引用與原創綜合敘述。草稿「AI 建議」的概念性近似改寫經嚴格估計約 10%～15%，連續文字重合更低，低於全文三分之一。
- 事實正確性：第二輪通過，剩餘必改 0。審查者核對需求書、OpenAI 官方頁、Java 原始碼、任務說明、交付紀錄與 Surefire 報告；桌上演練、案例既定需求與正式環境待確認事項已清楚分開。
- 總編輯：第二輪通過，可發布，剩餘必改 0。首次全名、標題層級、全形標點、初學者說明、系列銜接、Java 一致性與圖片規範均通過。
- 主文沒有 `AI 建議`、`作者撰寫區塊` 或 `待填` 等草稿標記。

## 作者發布前仍需確認

- [ ] 逐段改成作者確實做過、願意署名負責的第一人稱經驗；不可未經消化直接發布生成內容。
- [ ] 提交並推送主文、程式碼、交付文件與五張圖片後，逐一開啟 GitHub 與 raw 圖片網址。目前本機檔案都存在，但尚未推送，遠端連結不列為已驗證。
- [ ] 發布當日再次確認 OpenAI 官方頁、iThome 預覽、圖片顯示、字數與連結；需要修正時在當日內完成。
- [ ] 發布後不要刪除文章。
