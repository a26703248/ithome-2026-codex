# DAY06 三角色審查與發布前驗證

審查對象：`DAY06-給工程師的提示工程入門.md`

審查日期：2026-08-06

## 審查結論

| 審查角色 | 初審主要疑慮 | 處理結果 | 複核 |
|---|---|---|---|
| 著作權合規 | Day 07 銜接句與草稿內標為「AI 建議」的內容過近 | 重寫小結與銜接方式，不沿用「可審查的 OpenAPI 草稿」句型 | 通過 |
| 事實正確性 | 「ChatGPT 必須猜」過度斷言、無回應產物卻寫成實測、`List.copyOf` 的 `null` 元素邊界未揭露 | 改為條件式描述，移除無證據的回應敘述；提示詞明定拒絕 `null` 元素並新增測試 | 通過 |
| 讀者清晰度與規範 | Day 05 引句不精確、名詞首次出現未解釋、驗收敘述與測試不完全一致 | 核對前篇原文，補 Prompt Engineering、v0、record、Maven 與 Unix 紀元起點；修正驗收用詞 | 通過 |

## 著作權與引用檢查

- 文章未使用 ISO／CNS、法律條文或 OWASP 授權內容，不需讀取或比對 `條文/` 正本。
- OpenAI、JUnit 與 Java 官方文件均只做功能性事實轉述，沒有逐字引用或逐句翻譯。
- 保守把兩個提示詞／程式碼區塊及括號引文全數計入引用，約占可見內容 16%，低於全文三分之一。
- 五張圖片由 `圖檔/Day06/generate_day06_images.py` 從空白畫布產生，未使用第三方照片、圖示或插圖。
- 成稿未保留草稿中的「AI 建議」、作者撰寫提示或待填欄位。

## 事實與程式驗證

- OpenAI 官方資料支持「清楚、具體、提供背景及依回應反覆調整」的轉述；六欄檢查表已明確標示為作者自用方法，不是官方固定格式。
- v0 需求只確認兩種資料來源，欄位對齊規則仍待確認；文章中的欄位、預設值及 `null` 處理均標為本篇示範規則。
- `List.copyOf` 會建立不可修改的複本，並拒絕含 `null` 元素的來源清單；提示詞、實作、測試及正文敘述一致。
- `Instant.EPOCH` 是 Unix 紀元起點；文章沒有把它寫成已確認的業務預設值。
- 在 `程式碼/DAY06/` 執行 `mvn test`，結果為 `Tests run: 6, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。

## 格式與產物檢查

- 正文計入圖片替代文字約 1,092 個漢字；排除圖片行約 996 個，符合 900～1,100 字目標，也超過 300 字下限。
- 標題層次為一個 `#` 主標題，後續使用 `##`，沒有跳級。
- 五張圖片皆為 1280 × 720 px、16:9；文章使用 GitHub raw 網址，且每張圖片獨立成行。
- 範例統一使用 Java 17、Maven 與 JUnit 5，沒有混入其他程式語言。
- Day 05 開場引句與原文一致；Day 07 預告已改為檢查 HTTP 請求、回應與錯誤案例。

## 作者發布前確認

- [ ] 親自重跑三版提示詞；若要加入回應差異或量化結果，保存完整執行條件與回應紀錄後再寫入。
- [ ] 親自確認 `Instant.EPOCH`、拒絕標籤內 `null` 等示範規則符合想表達的案例，不把它們誤當正式業務決策。
- [ ] 文章上線當日再次打開 OpenAI、JUnit、Java 與 GitHub 連結。
- [ ] 圖片推送到 GitHub 後，確認五個 raw 網址能在 iThome 編輯器載入。
- [ ] 發布後若需修改，於發布當日完成。
