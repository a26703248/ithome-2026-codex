# Day 20 唯讀追蹤任務

```text
先不要修改任何檔案。請從 DailyReportJob.runAt() 開始，追蹤每日 08:00
產生 PDF 並寄信的完整路徑。

每個節點都列出：
1. 檔案與方法；
2. 輸入與輸出；
3. 是否讀取資料或產生副作用；
4. 支持結論的程式碼證據。

請把結論分成「已由程式碼確認」與「仍待執行期確認」。不要用類別名稱
推測業務語意。遇到排程器、依賴注入、環境變數、資料庫、PDF 套件或郵件
服務無法從這個範例確認時，列成問題，不要補空白。

最後列出：
- 修改這條路徑前應先讀的檔案；
- 應補的最小特徵測試；
- 重複執行時可能發生的副作用。
```
