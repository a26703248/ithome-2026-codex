# Day 06：資料來源正規化案例

本案例用同一個 Java 任務比較三版提示詞，並提供可執行的第三版參考實作。

## 執行方式

```bash
mvn test
```

環境：Java 17、Maven、JUnit 5。

## 驗證範圍

- 自行爬文資料欄位對應。
- 企業內部託管資料欄位對應。
- 字串、標籤與時間的示範預設值。
- 輸出標籤不受來源清單後續修改影響。
- 標籤清單含有 `null` 元素時會拒絕處理。
- `null` 來源物件會被拒絕。

`Instant.EPOCH` 等預設值只是 Day 06 的測試規則，不是原始需求書已確認的業務決策。
