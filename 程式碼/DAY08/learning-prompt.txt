我熟悉 Java 17 與 JUnit 5，正在學習 Spring cron 表示式與時區如何共同決定排程時刻。

情境：資料工作區需要「每日日報定時匯入」，但業務需求尚未確認時間與時區。本篇只用「每天上午九點」當學習範例，不得把它當成已確認需求。

請以反例出題者的方式協助：

1. 先給我一張空白預測表，欄位包含 cron、時區、現在時刻與下一次執行時刻。
2. 題目至少包含：Spring 六欄 cron、五欄 Linux crontab 格式、Asia/Taipei 與 Asia/Tokyo 的日期跨日情境。
3. 在我填完預測前，不要公布答案或完整程式。
4. 我回答後，逐格指出哪個前提有錯，再給一個能用 `CronExpression.parse` 與 `next` 驗證的 Java 骨架。
5. 我貼上 JUnit 5 輸出後，請將每個結論標成「Spring 官方文件」、「本篇工程假設」或「業務待確認」。
6. 最後列出 Spring 官方文件網址，並標出程式尚未驗證的邊界。

不得根據印象虛構 cron 欄位、`zone` 語意、框架預設時區或業務決策。
