# 計畫新增規則

## 目錄結構調整
- **`DayNN/verfiy.md`**: 文章需要人工驗證項目
- **`審核/DayNN/`**: 放置測試或驗證實驗案例資料
- **`DayNN.md`（根目錄）**：30 篇文章（`Day01.md`–`Day30.md`）。**檔案直接從 `# Day NN：標題` 開始，不要放 YAML frontmatter（`--- ... ---`）**——那會被 GitHub／ITHome 渲染成表格，正式文章不需要。成品要能整份直接貼上 ITHome 發布。撰稿時直接編輯對應檔案。
- **`程式碼/DayNN/`**：該天實作文章的可執行程式碼（見「實作 demo」慣例）。
- **`圖檔/DayNN/`**：每天文章的圖片目錄，當天所有圖片放進對應資料夾（見下方「文章配圖」的命名規則）。
- **`README.md`**：30 天大綱總表。**`CLAUDE.md`**：本規範。**`.env`**：金鑰（含 `OPENAI_API_KEY`），不得提交版控或以任何形式外洩。

## 三角色審查流程（每篇撰寫完、提交前必經）

**每寫完一篇文章，提交（git commit）前必須經過以下三個獨立子代理審查**（用 Agent 工具各派一個 general-purpose 子代理，可並行）。收到三份報告後，逐條處理有疑慮的點，通過才提交。這是硬性流程，不可略過。

1. **著作權合規審查**：確認**沒有逐字引用、也沒有貼近翻譯** ISO／CNS 等受著作權文件（**即使是翻譯也算抄襲**）。子代理須以 `條文/` 內對應正本逐句比對（正本多為 PDF，指示子代理用 scratchpad 的 venv ＋ pypdf 抽取文字比對），逐句判斷是「自己的話轉述」還是「貼近原文／翻譯」，標出風險等級與改寫建議。章節編號與標題、官方術語單詞屬事實，允許使用。
2. **事實正確性審查**：核對所有事實陳述（日期、條號、機關名稱、標題、採用關係、時序、概念主張）是否正確、轉述有無失真。同樣以 `條文/` 正本與可靠來源（必要時 WebSearch）佐證。**「換句話說也要說得正確」**——轉述不能扭曲原意。
3. **讀者清晰度與規範審查（總編輯）**：檢查讀者友善度（程式小白讀得懂、無跳步）、論文式名詞規範（首次全名、二次縮寫）、語氣（論文／出版書籍，非 AI／部落格腔）、標題層次（#→##→###、可直接發布、無「起承轉合」字樣）、起承轉合邏輯、**標點一致（全形中文標點）**、圖片規範（≥4–5 張、raw 網址、獨立成行）。

處理原則：著作權疑慮一律修到零風險；事實錯誤或術語不精確必改（優先採 `條文/` 正本的官方繁中術語）；編輯建議視情況採納，硬傷（錯字、破引用、名詞規範、標點不一致）必改。

## 文章配圖

- **每篇至少 4–5 張圖（硬性要求）**：避免整篇都是字，圖片幫助各類讀者理解。可用 **`gpt-image-2`** 生成概念／流程／意象圖，或用 **matplotlib** 繪製資料型圖表（長條圖、流程、對照），兩者皆可、可混用。每張圖都要在文章對應位置插入並附繁中 alt 文字。
- **命名與存放**：一律放 `圖檔/DayNN/`，檔名 `DayNN-序號-英文簡述.png`（序號兩位、由 01 起），一律 **16:9 橫向**。

以下為 `gpt-image-2` 生圖細節：

- **模型**：`gpt-image-2`（可直接生成 16:9 尺寸，不需事後裁切）。
- **金鑰**：從專案根目錄 `.env` 讀取 `OPENAI_API_KEY`。**絕不**把金鑰值寫進任何檔案、程式碼或對話輸出。
- **流程**：呼叫 OpenAI Images API 生圖 → 取回影像 → 直接寫檔到 `圖檔/DayNN/`。
- **存放與命名**：一律放對應的 `圖檔/DayNN/`；檔名 `DayNN-序號-英文簡述.png`（序號兩位、由 01 起），例如 `Day09-01-aims-overview.png`、`Day09-02-iso-cns-map.png`。
- **比例**：一律 **16:9 橫向**，直接用 `gpt-image-2` 產出 16:9。
- **圖中文字**：
    - 以**繁體中文**為主、**字數精簡**；圖片重點在「意象、概念、流程」的表達，不要塞滿說明文字。
    - 專有名詞與縮寫可**保留英文**（如 ISO/IEC 42001、CNS 42001、AIMS、RAG、OWASP、PDCA、NIST、SoA）。
- **在文章引用（用 raw 網址，非相對路徑）**：文章要能直接貼到 ITHome 等外部平台顯示，因此圖片一律用 GitHub raw 絕對網址，不用相對路徑（相對路徑只在 GitHub 頁面內顯示）。格式：
  `![繁中 alt](https://raw.githubusercontent.com/nickchen1998/ithelp-2026-ai-security/main/%E5%9C%96%E6%AA%94/DayNN/DayNN-序號-英文簡述.png)`
  其中 `%E5%9C%96%E6%AA%94` 是「圖檔」的 URL 編碼。圖片務必**獨立成行、前後留空行**，不要黏在文字段落中。（此網址需 repo 設為 public 才會生效；圖片檔仍照常存放於本機 `圖檔/DayNN/` 並提交。）

