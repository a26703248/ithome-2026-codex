# Day 29｜踩雷經驗談：把事故翻譯成護欄

![Day 29 封面：事故要翻譯成護欄，不是翻譯成一句下次小心](https://raw.githubusercontent.com/a26703248/ithome-2026-codex/main/%E5%9C%96%E6%AA%94/Day29/day29-01-cover.png)

Day 28 那句「Day 29 會回看系列裡沒有成功的案例」，我最先想到的不是哪一次程式碼寫錯，而是 Day 26 那位同事拿真實客戶掃描檔案補 OCR 測試的事。當時我把它寫進治理盤點表，處理的是「該不該用這份資料」；今天要問的是另一題：為什麼會走到這一步、下次怎麼提早擋下來。這篇不是要列一串「Codex 又出包」的清單——工具沒有主動去抓客戶檔案，是任務交辦時沒人劃出「哪些資料能給它碰」的線。真正該檢討的是任務範圍、上下文、驗收條件這些我們自己能改的條件。

## 三類失敗，其實是同一個問題的三個階段

回看 Day 01 到 Day 28，真正讓我停下來的事故都落在三個階段：交辦當下資訊不夠、執行時改動範圍比預期廣、驗收時把「看起來完成」當成「真的完成」。三者常常連在一起——上下文不足讓 Codex 自己補假設，補錯的假設讓修改範圍跟著跑偏，範圍跑偏又更容易被一句「已完成」蓋過去。

![三類失敗模式與對應案例：Day 26、Day 27、Day 24](https://raw.githubusercontent.com/a26703248/ithome-2026-codex/main/%E5%9C%96%E6%AA%94/Day29/day29-02-three-failure-modes.png)

## 失敗一：需求與上下文不足——Day 26 那次事故重新拆解

交辦寫的是「幫忙補 OCR（Optical Character Recognition，光學字元辨識）／ETL（Extract-Transform-Load，資料擷取轉換載入）前處理的單元測試」，沒說測試資料能不能用真實客戶掃描檔。需求書第五節把「資料敏感度分級」列為待確認，那份分級清單當時公司內部根本還沒訂出來，交辦時也沒提醒這個空白。我事後翻測試檔案，斷言字串裡直接出現客戶掃描檔的檔名與內容片段，不是合成資料——程式本身沒有錯，Codex 只是照著能拿到的檔案做事，問題出在沒人告訴它「這批檔案不能用」。Day 26 那張使用案例盤點表，「輸入資料」欄位現在是交辦前必填項，沒有分級結果就先用去識別化的合成資料頂著；權限盤點也把可讀寫目錄限定在測試資料夾，不含正式掃描檔存放區。

## 失敗二：修改過廣——一次「順手」的列舉改名

這是延續 Day 27 那支 `OcrPreprocessor` 程式碼另外設計的情境，Day 27 當時並沒有發生這次事故。我請 Codex 幫 `REJECT` 分支補一段退回時間戳記，方便回填 Day 26 的稽核紀錄，交辦裡寫明「只改 `evaluate()` 的 REJECT 分支與對應測試」。Codex 回報完成後，我照 Day 14 學到的習慣去看 diff，檔案數不對：預期 1～2 個，實際變更了 5 個。它覺得 `MANUAL_REVIEW` 這個名稱不夠精準，「順手」把整個列舉改名成 `NEEDS_REVIEW`，連 `OcrOutcome.java`、8 項既有測試斷言，還有同一個 ETL 模組裡另外兩處依 `OcrOutcome` 分流、寫批次退回報表的程式碼都一起被改掉，那兩處根本不在允許清單內。

```java
Set<String> allowedFiles = Set.of(
        "OcrPreprocessor.java", "OcrPreprocessorTest.java");
Set<String> touchedFiles = diff.changedFiles();
touchedFiles.removeAll(allowedFiles);
if (!touchedFiles.isEmpty()) {
    throw new IllegalStateException("超出允許範圍的檔案：" + touchedFiles);
}
```

若沒核對檔案數就合併，那兩處報表原本比對 `MANUAL_REVIEW` 字樣的邏輯會跟新列舉對不上，退回原因的統計直接壞掉。我退回這次變更，只留下時間戳記那段，列舉改名另外拆成一個要跨模組討論的獨立任務。檔案數量不能取代逐行審查，但確實是最快抓出範圍跑掉的訊號。

![允許改動的檔案清單，對上實際 diff 變更的 5 個檔案](https://raw.githubusercontent.com/a26703248/ithome-2026-codex/main/%E5%9C%96%E6%AA%94/Day29/day29-03-diff-scope.png)

## 失敗三：把完成摘要當成驗收——Day 24 交付包被誤讀

Day 24 結尾寫得清楚：「Java 程式已能拒絕未填複核原因的提交、保留原稿；敏感內容遮蔽、畫面失敗狀態、正式授權與監控仍列為待確認。」那份交付包裡「4 項測試全部通過」只涵蓋複核流程的程式契約。

這段後續是我依真實交付包結構另外補的情境，Day 24 本身沒有寫到展示排程：PM 排展示行程時只看了交付包裡「已確認」區塊與綠色測試結果，沒往下讀「待角色確認」那一欄——遮蔽規則、畫面失敗狀態、正式授權與監控都還列在那裡，就把功能排進客戶展示日程。我在展示前重新核對交付包，確認四項確實都還沒實作，才把展示範圍改成「複核流程 Demo，不含正式資料」，待確認四項另外標成獨立待辦、指定負責角色。完成摘要沒有說謊，「已確認」跟「已完成」之間那段距離，卻很容易被排行程的人直接跳過；往後我在交付包最上面加一行「程式契約完成，四項待角色確認」。

## 從事故回推護欄

三次事故對應到不同時間點就能擋下來的動作，缺一個都不夠：

| 失敗模式 | 預防 | 偵測 | 限制影響 | 復原 |
|---|---|---|---|---|
| 上下文不足（Day 26 客戶掃描檔） | 交辦前填資料分級與允許目錄 | 覆核測試斷言是否出現真實檔名 | 限定可讀寫目錄到測試資料夾 | 換成合成資料、重跑測試 |
| 修改過廣（列舉改名） | 交辦寫明允許改動的檔案清單 | 核對 diff 變更檔案數是否超出預期 | 只合併允許清單內的變更 | 退回多餘變更、拆成獨立任務 |
| 摘要誤導（交付包被誤讀） | 標題直接寫「完成／待確認」項目數 | 展示或上線前重讀待確認清單 | 縮小展示或發布範圍 | 標成部分完成、列出負責人 |

![事故回推護欄矩陣：失敗模式對應預防、偵測、限制影響、復原](https://raw.githubusercontent.com/a26703248/ithome-2026-codex/main/%E5%9C%96%E6%AA%94/Day29/day29-04-guardrail-matrix.png)

## 效益與注意事項

攤開這三次事故後，我更新的不是心情，是三個具體東西：交辦模板多一欄「允許改動範圍」、diff 檢查多一道「變更檔案數」門檻、交付包標題不能只寫「完成」。公開分享失敗案例仍要顧到分寸：客戶檔名、同事身分、專案代號都拿掉，只留任務範圍、觸發條件與程式層級的證據；也不能拿這三次事故說「Codex 不可靠」，根因其實都在交辦與驗收這端。

![更新後的交辦與驗收流程：三個新增檢查點](https://raw.githubusercontent.com/a26703248/ithome-2026-codex/main/%E5%9C%96%E6%AA%94/Day29/day29-05-updated-workflow.png)

## 小結與 Day 30 預告

失敗案例要能被下一個人重複使用，得先變成可觀察的警訊、可執行的預防動作，一句「下次小心」不算數。Day 24 到 Day 29 用「AI 輔助生成系統」這套案例，走完跨角色協作、Prompt／SOP、治理、成本效能與數據量測，也在今天回頭補上三次事故留下的護欄。Day 30 會把這 30 天累積的方法、數據與限制收攏起來，整理成下一步能持續執行的行動清單。

## 參考資料

- `案例/AI輔助生成系統-需求書.md`
- 本系列 Day 24、Day 26、Day 27 的交付包、盤點表與 diff 審查紀錄
